<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0&icon_names=arrow_forward,check" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>
<body>
  <%- include('partials/header') %>
  <main class="background-frame">

    <div class="form__container form__container--auth">
      <form class="form" action="/auth/registration" method="POST">
        <div class="form__header">
          <h2>Sign Up</h2>
        </div>
        <label class="form__label--text">
          <b>Your Email:</b>
          <input type="email" name="email" value="<%= email ? email : '' %>" class="<%= errors && errors.email ? 'input--error' : '' %>" maxlength="255" required>
          <% if (errors && errors.email) { %>
            <div class="error-message"><%= errors.email.msg %></div>
          <% } %>
        </label>

        <label class="form__label--text">
          <b>Username:</b>
          <input type="text" name="username" value="<%= username ? username : '' %>" class="<%= errors && errors.username ? 'input--error' : '' %>" maxlength="255" required>
          <% if (errors && errors.username) { %>
            <div class="error-message"><%= errors.username.msg %></div>
          <% } %>
        </label>

        <label class="form__label--text">
          <b>Enter Password:</b>
          <input type="password" name="password" class="<%= errors && errors.password ? 'input--error' : '' %>" minlength="8" maxlength="255" required>
          <% if (errors && errors.password) { %>
            <div class="error-message"><%= errors.password.msg %></div>
          <% } %>
        </label>

        <label class="form__label--text">
          <b>Confirm Password:</b>
          <input type="password" name="passwordConfirm" class="<%= errors && errors.password ? 'input--error' : '' %>" minlength="8" maxlength="255" required>
          <% if (errors && errors.passwordConfirm) { %>
            <div class="error-message"><%= errors.passwordConfirm.msg %></div>
          <% } %>
        </label>

        <button type="submit">
          Register
        </button>

        <div class="form__auth">
          <p>Already have an account?</p>
          <a class="link-animated" href="/auth/login">
            <h3>Log In</h3>
            <span class="material-symbols-outlined">
              arrow_forward
            </span>
          </a>
        </div>

      </form>
    </div>
  </main>
  <%- include('partials/footer') %>
</body>
</html>